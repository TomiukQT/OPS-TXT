-Instalace serveru s grafickým jádrem

-2 síové karty > - bridge
		  - vnitøní sí intnet
-nastavení jména + domény (server.valenta.local)
-reboot

-nastavení síových karet
	1) s konektivitou = ISP/WAN
	-opsat si ip adresu a gateway
	-nastavit ip na statickou (opsanou) + maska + gateway + DNS (8.8.8.8,8.8.4.4)

	2) bez konektivity = LAN
		nastavit statickou IP ze zadání (10.255.13.1 / 24) a masku

-instalace smìrování
- Správce serveru > správa > pøidat role a funkce > role serveru (vzdálený pøístup) > služby rolí (zaškrtnou smìrování) > nainstalovat
		  > nástroje > smìrování a vzdálený pøístup > "SERVER" (nakonfigurovat a povolit smìrování a ...) 
				> pøeklad adres (NAT) > zvolit ISP/WAN (karta s konektivitou) > dokonèit

-instalace DHCP
- Správce serveru > správa > pøidat role a funkce > role serveru (server dhcp) > nainstalovat
		  > nástroje > DHCP > server > IPv4 (Nový obor..) > nastavit rozsah (podle zadání) a masku [10.255.13.10 - 10.255.13.254; 255.255.255.0]
				>trvání zápùjèky > smìrovaè (ip adresa serveru = 10.255.13.1) > dokonèit
	

-instalace DNS
- Správce serveru > správa > pøidat role a funkce > role serveru (server dns) > nainstalovat¨
		  -karta ISP > nastavit dns na 127.0.0.1 (v cmd command nslookup)
		  > nástroje > DHCP > server > IPv4  > Obor > možnosti oboru > 006 Servery DNS > odebrat všechny, pøidat adresu serveru (10.255.13.1)
		  > nástroje > DHCP > server (všechyn úkoly > restartovat)
	zkusit clienta (zkontrolovat konektivitu a ip adresy a nslookup)

-instalace ADDS		
- Správce serveru > správa > pøidat role a funkce > role serveru (služba active directory domain services) > nainstalovat
		  > vlajeèka > povýšit na øadiš domény > konfigurace nasazení - pøidat novou doménovou strukturu (název = ze zadání (valenta.local))
				> možnosti øadièe - nastavit heslo > instalovat > reboot
		  > vlajeèka > dokonèit konfiguraci složky DHCP > pøeskoèit autorizaci
	zkusit na clientovi pøihlásit se do domény () > reset client

-nastavení uživatelù,domácích diskù, cestovních profilù a kvót
na disku C 2 nové složky (Homes a Profiles) > na obì right click > sdílet s > urèití lidé 
		> rozkliknout search bar a zvolit najít lidi > Zadejte názvy objektù = Authenticated Users > OK  
		> úroveò oprávnìní u Auth. Users nastavit na ètení/zápisù > sdílet

- Správce serveru > správa > uživatelé a poèítaèe služby AD > server > users (nová položka > uživatel) 
			>nastavení jména, uživatelského jména a hesla (user1) 
			>user1 (right click - Vlastnosti) > profil 
			> Cesta k profilu = cesta k složce profiles + uživatelsé jméno (//SERVER/Profiles/user1)
			> Domovská složka  > pøipojit k = cesta k složce homes + uživatelsé jméno (//SERVER/Homes/user1)
			
		Zkusit na clientu
Kvóty: disk c > vlastnosti > Kvóta > zaškrtnout > povolit správu a odepøít místo na disku (mùžeme i zaškrtnou možnosti protokolování)
				   > Omezit místo na disku a nastavit kvóty (nebo pøidìlené kvóty a najít si uživatele a nastavit kvótu jenom jemu)			